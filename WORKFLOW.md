# 后续工作流程

## 已确认的赛事默认值

- 默认采用原项目第二种 **Match 赛事模式：每队 4 Ban、5 Pick**，共 18 步。
- 服务器默认值为 `src/shared/types.ts` 的 `draftMode: 'match'`；Reset Match 使用同一默认值。
- 保留 2 Ban 模式供需要时手动选择，不作为默认模式。

## 执行顺序

1. 完成服务器、共享状态、REST/WebSocket、三端页面及权限隔离。
2. 完成延迟时间轴、Undo/Reset、持久化、刷新恢复和自动重连。
3. 完成构建、代码检查、服务器测试、三端浏览器测试与显示检查。
4. 准备启动、备份恢复、OBS 接入与部署说明；实际设备/云端彩排按资源可用情况另行进行。
5. **最后进行英雄数据核对与同步准备（用户于 2026-09-15 指定）。**
   - 读取原项目 `vite-project/src/components/HeroList.tsx` 及 `public/heroesImg/`。
   - 将该文件视为旧数据，不能因为页面可用就视为英雄名单已同步。
   - 以 Honor of Kings 国际服为范围，优先核对官方英雄名单、版本更新和上线公告。
   - 列出本地缺失英雄、别名/名称差异、已有数据问题；区分已上线、预告、限时与无法确认项。
   - 每份结果标注核查时间、可追溯来源和覆盖限制，不把国服或其他游戏名单当国际服现有名单。
   - 先产出差异清单，再据证据补名称/头像/位置；缺乏来源的 Counter/Combo/Be Countered 不编造。
   - 同步时保留现有 ID，新增英雄使用新 ID，避免破坏历史 BP 和关系引用。

本轮第 1–4 项的本地实现和检查已完成，最后一项核对结果见 `HERO-DATA-AUDIT.md`。
